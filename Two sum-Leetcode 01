class Solution:
    def twoSum(self,num,target):
        d={}
        for i in range (len(num)):
            rem=target-num[i]
            if rem in d:
                return [d[rem],i]
            d[num[i]]=i    
